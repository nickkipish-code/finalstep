# Настройка Google Gemini 2.5 Flash Image

## Что такое Gemini 2.5 Flash Image?

**Gemini 2.5 Flash Image** (также известная как "Nano Banana" 🍌) - это экспериментальная модель от Google для генерации и редактирования изображений.

## Получение API ключа

1. Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войдите в свой Google аккаунт
3. Нажмите "Create API Key"
4. Скопируйте ключ (начинается с `AIzaSyC...`)

## Настройка проекта

1. Создайте файл `backend/.env`:
   ```
   GEMINI_API_KEY=ваш_ключ_здесь
   ```

2. Убедитесь, что файл `.env` добавлен в `.gitignore` (не коммитьте ключи!)

3. Установите зависимости:
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

## Проверка работы

Запустите backend:
```bash
cd backend
python main.py
```

Проверьте health endpoint:
```bash
curl http://localhost:8000/health
```

Должен вернуться JSON с `"gemini_ready": true`.

## Квоты и лимиты

**Free Tier:**
- 60 запросов в минуту
- 1,500 запросов в день
- 1.5 миллиона запросов в месяц
- **БЕСПЛАТНО**

Источник: [Google AI Pricing](https://ai.google.dev/pricing)

## Важные замечания

⚠️ **Экспериментальная модель:**
- Может быть нестабильной
- Доступна не во всех регионах
- Может требовать особых прав
- Не всегда возвращает изображение

**Fallback стратегия:**
Если модель не вернула изображение, backend возвращает оригинал с watermark.

## Тестирование API

Используйте `backend/backend_test.py`:
```bash
cd backend
python backend_test.py
```

Или используйте curl:
```bash
curl -X POST http://localhost:8000/api/try-on/text \
  -F "person_image=@photo.jpg" \
  -F "clothing_description=красное платье" \
  --output result.png
```

## Troubleshooting

### "GEMINI_API_KEY не найден"
- Проверьте файл `backend/.env`
- Убедитесь, что ключ правильный
- Перезапустите backend

### "Модель не инициализирована"
- Проверьте интернет соединение
- Проверьте валидность API ключа
- Проверьте доступность модели в вашем регионе

### "Модель не вернула изображение"
- Это нормально для экспериментальной модели
- Попробуйте другой промпт
- Проверьте качество входного изображения
- Fallback вернёт оригинал с watermark

## Дополнительные ресурсы

- [Google AI Documentation](https://ai.google.dev/docs)
- [Gemini API Reference](https://ai.google.dev/api)
- [Awesome Nano Banana](https://github.com/JimmyLv/awesome-nano-banana)

