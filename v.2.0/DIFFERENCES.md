# Отличия v2.0 от v1.0

## Архитектура

### v1.0 (старая версия)
```
Primer/
├── backend/          # Python + FastAPI
│   ├── main.py
│   └── requirements.txt
├── frontend/         # Next.js Frontend
│   └── app/
```
- Два отдельных сервера (Python на :8000, Next.js на :3000)
- Требуется Python 3.9+ и Node.js 18+
- Два процесса запуска

### v2.0 (новая версия)
```
v.2.0/
├── app/
│   ├── api/         # Next.js API Routes (вместо Python)
│   └── page.tsx
├── lib/
│   └── gemini/      # TypeScript утилиты
```
- Единое приложение на Next.js
- Требуется только Node.js 18+
- Один процесс запуска

## Технологии

| Компонент | v1.0 | v2.0 |
|-----------|------|------|
| Backend | Python + FastAPI | Next.js API Routes |
| AI SDK | `google-generativeai` (Python) | `@google/generative-ai` (TypeScript) |
| Обработка изображений | PIL/Pillow | Sharp |
| Веб-скрейпинг | BeautifulSoup + Playwright | Cheerio + Playwright |
| Типизация | Частичная (frontend) | Полная (TypeScript) |

## API Endpoints

### v1.0
```
http://localhost:8000/api/try-on/text
http://localhost:8000/api/try-on/image
http://localhost:8000/api/try-on/url
http://localhost:8000/api/try-on/background
```

### v2.0
```
/api/try-on/text
/api/try-on/image
/api/try-on/url
/api/try-on/background
```
- Относительные пути (работают из того же домена)
- Нет CORS проблем
- Единый порт

## Запуск

### v1.0
**PowerShell:**
```powershell
# Terminal 1 - Backend
cd backend
.\venv\Scripts\Activate.ps1
python main.py

# Terminal 2 - Frontend  
cd frontend
npm run dev
```

**CMD:**
```cmd
# Terminal 1
cd backend
venv\Scripts\activate
python main.py

# Terminal 2
cd frontend
npm run dev
```

### v2.0
**Любая оболочка:**
```bash
npm run dev
```

## Зависимости

### v1.0
**Backend (Python):**
- fastapi
- uvicorn
- pillow
- google-generativeai
- python-multipart
- beautifulsoup4
- playwright

**Frontend (Node.js):**
- next
- react
- axios
- framer-motion
- и другие

### v2.0
**Только Node.js:**
- next
- react
- @google/generative-ai
- sharp
- cheerio
- playwright
- framer-motion
- и другие

## Преимущества v2.0

### 🚀 Производительность
- Меньше накладных расходов (один сервер вместо двух)
- Server-side рендеринг (SSR)
- Автоматическая оптимизация изображений

### 💼 Разработка
- Единая кодовая база (TypeScript)
- Лучшая типизация
- Проще отладка
- Меньше конфигурационных файлов

### 🌐 Деплой
- Проще деплой (одно приложение)
- Vercel / Netlify поддержка out of the box
- Не нужен отдельный Python сервер
- Меньше настроек окружения

### 🔧 Поддержка
- Единый package.json
- Единые node_modules
- Проще обновления зависимостей

## Недостатки v2.0

### ⚠️ Размер bundle
- Больше JavaScript кода
- Все библиотеки в Node.js (sharp, playwright)

### 🔄 Миграция
- Нужно переписать Python код на TypeScript
- Изменение структуры проекта

## Когда использовать какую версию?

### Используйте v1.0 если:
- У вас уже есть Python инфраструктура
- Нужна максимальная гибкость backend
- Требуется использовать специфичные Python библиотеки

### Используйте v2.0 если:
- Начинаете с нуля
- Хотите простоту разработки и деплоя
- Предпочитаете TypeScript и полную типизацию
- Нужен простой деплой на Vercel/Netlify

## Миграция с v1.0 на v2.0

Если вы используете v1.0 и хотите мигрировать:

1. Сохраните данные пользователей (если есть)
2. Установите Node.js 18+
3. Скопируйте папку v.2.0
4. Настройте `.env.local` с теми же ключами
5. Запустите `npm install`
6. Запустите `npm run dev`
7. Протестируйте все функции

**Время миграции:** ~10-15 минут

## Совместимость API

API endpoints полностью совместимы. Если у вас есть клиенты, использующие v1.0 API, они будут работать с v2.0 после изменения базового URL с `http://localhost:8000` на `http://localhost:3000`.

## Заключение

**v2.0** - это современная, упрощенная версия приложения, которая легче в разработке, деплое и поддержке. Рекомендуется для новых проектов.

**v1.0** остается актуальной для проектов с существующей Python инфраструктурой.

