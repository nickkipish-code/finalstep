# Virtual Fitting Room v2.0 - Полностью на Next.js 🚀

Виртуальная примерочная с искусственным интеллектом - полностью переписано на Next.js с интегрированным backend.

## 🎯 Что нового в версии 2.0

- ✅ **Единое приложение на Next.js** - backend и frontend в одном проекте
- ✅ **Next.js API Routes** - вместо отдельного Python backend
- ✅ **Полная типизация TypeScript**
- ✅ **Серверные компоненты** - оптимизированная производительность
- ✅ **Упрощенное развертывание** - одна команда `npm run dev`
- ✅ **Все функции оригинала** - text-to-image, image-to-image, URL парсинг, смена фона

## 🚀 Быстрый старт

### Требования

- **Node.js 18+** ([скачать](https://nodejs.org/))
- **Google Gemini API ключ** ([получить бесплатно](https://aistudio.google.com/app/apikey))

### Установка

1. **Клонируйте или перейдите в папку проекта**
   ```bash
   cd v.2.0
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   
   Создайте файл `.env.local` в корне проекта:
   ```bash
   # Google Gemini API (ОБЯЗАТЕЛЬНО)
   GEMINI_API_KEY=your_api_key_here
   ```

   Получите ключ здесь: https://aistudio.google.com/app/apikey

4. **Установите Playwright (для парсинга ссылок)**
   ```bash
   npx playwright install chromium
   ```

### Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
v.2.0/
├── app/                          # Next.js App Router
│   ├── api/                      # API Routes (backend)
│   │   ├── health/              # Health check
│   │   └── try-on/              # Основные endpoints
│   │       ├── text/            # Text to Image
│   │       ├── image/           # Image to Image
│   │       ├── url/             # URL парсинг
│   │       └── background/      # Смена фона
│   ├── globals.css              # Глобальные стили
│   ├── layout.tsx               # Root layout
│   └── page.tsx                 # Главная страница
│
├── components/                   # React компоненты
│   ├── FittingRoom.tsx          # Главный компонент примерочной
│   ├── ImageCanvas.tsx          # Просмотр и редактирование результата
│   ├── Header.tsx               # Шапка сайта
│   ├── Hero.tsx                 # Hero секция
│   ├── Features.tsx             # Список возможностей
│   ├── Footer.tsx               # Подвал
│   └── NeonGridBackground.tsx   # Анимированный фон
│
├── contexts/                     # React Contexts
│   └── ThemeContext.tsx         # Переключение тем
│
├── lib/                          # Утилиты и библиотеки
│   ├── gemini/                  # Google Gemini интеграция
│   │   ├── config.ts            # Конфигурация API
│   │   ├── generators.ts        # Генерация изображений
│   │   └── image-processing.ts # Обработка изображений
│   └── utils/                   # Утилиты
│       └── scraper.ts           # Парсинг веб-страниц
│
├── public/                       # Статические файлы
├── .env.local.example           # Пример конфигурации
├── package.json                 # Зависимости
├── tsconfig.json                # TypeScript конфиг
└── README.md                    # Этот файл
```

## 🎨 Возможности

### 1. **Text to Image** 📝
Опишите одежду текстом:
- "Элегантное красное платье до колен"
- "Черный кожаный пиджак с металлическими молниями"
- "Синие джинсы skinny fit"

### 2. **Image to Image** 🖼️
Загрузите фото одежды - AI перенесет её на ваше фото

### 3. **URL Парсинг** 🔗
Вставьте ссылку на товар с маркетплейса:
- Prom.ua
- OLX.ua
- Temu.com
- И другие

### 4. **Background Change** 🌆
Измените фон изображения:
- "Пляж с белым песком"
- "Городская улица ночью"
- "Горы на закате"

### 5. **Смена ракурса** 📐
При смене фона можно указать новый ракурс камеры:
- "Вид сбоку"
- "Низкий ракурс"
- "Крупный план"

## 🎭 Темы

Приложение поддерживает две темы:
- **Classic** - классическая фиолетовая тема
- **Neon** - неоновая киберпанк тема с анимацией

Переключение через кнопку 🎨 в header.

## ⚙️ API Endpoints

### Health Check
```
GET /api/health
```

Ответ:
```json
{
  "status": "healthy",
  "gemini_ready": true,
  "model": "gemini-2.0-flash-exp",
  "version": "2.0.0"
}
```

### Text to Image
```
POST /api/try-on/text
Content-Type: multipart/form-data

{
  "person_image": File,
  "clothing_description": string
}
```

### Image to Image
```
POST /api/try-on/image
Content-Type: multipart/form-data

{
  "person_image": File,
  "clothing_image": File,
  "description": string (optional)
}
```

### URL Parsing
```
POST /api/try-on/url
Content-Type: multipart/form-data

{
  "person_image": File,
  "product_url": string,
  "description": string (optional)
}
```

### Background Change
```
POST /api/try-on/background
Content-Type: multipart/form-data

{
  "person_image": File,
  "background_description": string,
  "camera_angle": string (optional)
}
```

## 🔧 Конфигурация

### Переменные окружения (.env.local)

```bash
# Google Gemini API (ОБЯЗАТЕЛЬНО)
GEMINI_API_KEY=your_key_here

# NextAuth.js (Опционально - для авторизации в будущем)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key

# Google OAuth (Опционально)
GOOGLE_CLIENT_ID=your-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-secret

# Supabase (Опционально - для сохранения истории)
NEXT_PUBLIC_SUPABASE_URL=your-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-key
```

### Настройки модели (lib/gemini/config.ts)

```typescript
export const MODEL_NAMES = {
  FLASH_IMAGE: 'gemini-2.0-flash-exp',  // Основная модель
  FLASH: 'gemini-1.5-flash',            // Fallback
  PRO: 'gemini-1.5-pro'                 // Для будущего использования
}

export const GEMINI_CONFIG = {
  maxRetries: 3,              // Количество попыток при ошибке
  baseRetryDelay: 16,         // Задержка между попытками (секунды)
  timeout: 60000,             // Таймаут запроса (мс)
}
```

## 🐛 Troubleshooting

### "Gemini API не настроен"
- Проверьте файл `.env.local`
- Убедитесь что ключ начинается с `AIzaSy...`
- Перезапустите dev сервер

### "Не удалось загрузить модель"
- Проверьте подключение к интернету
- Проверьте что ключ API валиден
- Fallback модель загрузится автоматически

### "Превышена квота API"
- Бесплатный tier: 60 запросов/мин, 1,500/день
- Подождите или обновите тариф

### Ошибки при парсинге URL
- Убедитесь что URL доступен
- Некоторые сайты блокируют скрейпинг
- Используйте прямые ссылки на товары

### Playwright не установлен
```bash
npx playwright install chromium
```

## 📦 Сборка и деплой

### Production сборка
```bash
npm run build
npm run start
```

### Деплой на Vercel
1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Деплой:
   ```bash
   vercel
   ```

3. Добавьте переменные окружения в Vercel Dashboard:
   - `GEMINI_API_KEY` - обязательно
   - Другие по необходимости

### Требования к хостингу
- Node.js 18+
- Поддержка Edge Runtime (для оптимизации)
- Минимум 512MB RAM

## 🔗 Полезные ссылки

- [Google AI Studio](https://aistudio.google.com/app/apikey) - Получить API ключ
- [Gemini Documentation](https://ai.google.dev/docs) - Документация
- [Next.js Docs](https://nextjs.org/docs) - Next.js документация
- [Vercel](https://vercel.com) - Бесплатный хостинг

## 🚀 Производительность

- **Время генерации**: 5-15 секунд
- **Размер бандла**: ~500KB (gzipped)
- **Lighthouse Score**: 95+
- **Оптимизированные изображения**: Sharp для обработки

## 📝 Лицензия

MIT License

## 🙏 Благодарности

- **Google Gemini** - AI движок
- **Next.js** - React фреймворк
- **Vercel** - Хостинг
- **Sharp** - Обработка изображений
- **Playwright** - Веб-скрейпинг

---

**Создано с ❤️ используя Next.js + Google Gemini**

## 💡 Советы по использованию

1. **Качество фото** - используйте четкие фото людей без сильных теней
2. **Описания** - будьте подробны в описаниях одежды
3. **Ссылки** - используйте прямые ссылки на товары с фото
4. **Фон** - для лучших результатов описывайте фон подробно
5. **Ракурс** - экспериментируйте с разными ракурсами камеры

Наслаждайтесь виртуальной примерочной! 🎉

