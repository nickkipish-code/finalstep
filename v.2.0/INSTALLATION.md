# 📦 Полная инструкция по установке

## Системные требования

- **Операционная система:** Windows 10/11, macOS 10.15+, Linux (Ubuntu 20.04+)
- **Node.js:** версия 18.17.0 или выше
- **Память:** минимум 4GB RAM (рекомендуется 8GB)
- **Место на диске:** ~2GB для node_modules и зависимостей

## Шаг 1: Установка Node.js

### Windows
1. Скачайте установщик: https://nodejs.org/
2. Запустите установщик
3. Следуйте инструкциям (установите все компоненты)
4. Проверьте установку:
   ```cmd
   node --version
   npm --version
   ```

### macOS
```bash
# Используя Homebrew
brew install node

# Или скачайте с nodejs.org
```

### Linux (Ubuntu/Debian)
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## Шаг 2: Клонирование проекта

Если проект в Git:
```bash
git clone <repository-url>
cd v.2.0
```

Или просто перейдите в папку v.2.0:
```bash
cd v.2.0
```

## Шаг 3: Установка зависимостей

### Основные зависимости
```bash
npm install
```

Это установит все необходимые пакеты из `package.json`.

### Playwright (для парсинга ссылок)
```bash
npx playwright install chromium
```

Если возникли проблемы на Windows, попробуйте:
```powershell
$env:PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
npm install playwright
npx playwright install chromium --with-deps
```

## Шаг 4: Получение Gemini API ключа

1. Перейдите на https://aistudio.google.com/app/apikey
2. Войдите с Google аккаунтом
3. Нажмите "Create API Key" (или "Get API Key")
4. Выберите проект (или создайте новый)
5. Скопируйте созданный ключ (формат: `AIzaSy...`)

## Шаг 5: Настройка переменных окружения

### Способ 1: Копирование шаблона (рекомендуется)

**Windows (PowerShell):**
```powershell
Copy-Item .env.local.template .env.local
```

**Windows (CMD):**
```cmd
copy .env.local.template .env.local
```

**macOS/Linux:**
```bash
cp .env.local.template .env.local
```

### Способ 2: Создание вручную

Создайте файл `.env.local` в корне проекта:

```bash
GEMINI_API_KEY=AIzaSy_ваш_ключ_здесь
```

### Редактирование файла

Откройте `.env.local` в любом текстовом редакторе и замените `AIzaSy_ваш_ключ_здесь` на ваш реальный ключ.

**Windows:**
```cmd
notepad .env.local
```

**macOS:**
```bash
open -e .env.local
```

**Linux:**
```bash
nano .env.local
# или
gedit .env.local
```

## Шаг 6: Проверка конфигурации

Убедитесь, что файл `.env.local` содержит валидный ключ:

```bash
# Windows PowerShell
Get-Content .env.local

# Windows CMD
type .env.local

# macOS/Linux
cat .env.local
```

Должно быть что-то вроде:
```
GEMINI_API_KEY=AIzaSyB1234567890abcdefghijklmnopqrstuv
```

## Шаг 7: Запуск приложения

### Development режим
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Production режим
```bash
npm run build
npm run start
```

## Проверка работоспособности

1. Откройте http://localhost:3000
2. Нажмите "Начать примерку"
3. Загрузите тестовое фото человека
4. Введите описание одежды, например: "красное платье"
5. Нажмите "Примерить"

Если все работает - поздравляем! 🎉

## Устранение проблем

### Проблема: "Cannot find module"

**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Port 3000 is already in use"

**Решение:**

**Windows:**
```powershell
# Найти процесс на порту 3000
netstat -ano | findstr :3000

# Убить процесс (замените PID на реальный)
taskkill /PID <PID> /F

# Или запустите на другом порту
$env:PORT=3001; npm run dev
```

**macOS/Linux:**
```bash
# Найти и убить процесс
lsof -ti:3000 | xargs kill -9

# Или запустите на другом порту
PORT=3001 npm run dev
```

### Проблема: "GEMINI_API_KEY не найден"

**Решение:**
1. Проверьте, что файл `.env.local` существует в корне проекта
2. Убедитесь, что файл содержит строку `GEMINI_API_KEY=...`
3. Убедитесь, что нет лишних пробелов или кавычек
4. Перезапустите сервер разработки

### Проблема: Playwright не установлен

**Решение:**
```bash
npx playwright install chromium --with-deps
```

Если не помогло:
```bash
# Полная переустановка
npm uninstall playwright
npm install playwright@latest
npx playwright install chromium --force --with-deps
```

### Проблема: Ошибки TypeScript

**Решение:**
```bash
# Очистить кэш TypeScript
rm -rf .next
npm run build
```

### Проблема: "Sharp" ошибки на Windows

**Решение:**
```bash
npm uninstall sharp
npm install --platform=win32 --arch=x64 sharp
```

## Дополнительные настройки

### Изменение порта
Создайте/отредактируйте файл `.env.local`:
```bash
PORT=3001
```

### Включение debug режима
```bash
# .env.local
NODE_ENV=development
DEBUG=*
```

### Настройка лимитов памяти
```bash
# Windows PowerShell
$env:NODE_OPTIONS="--max-old-space-size=4096"
npm run dev

# macOS/Linux
NODE_OPTIONS="--max-old-space-size=4096" npm run dev
```

## Обновление зависимостей

### Проверка устаревших пакетов
```bash
npm outdated
```

### Обновление всех пакетов
```bash
npm update
```

### Обновление конкретного пакета
```bash
npm install <package-name>@latest
```

## Следующие шаги

После успешной установки:

1. Прочитайте [README.md](./README.md) для полного понимания возможностей
2. Изучите [QUICK_START.md](./QUICK_START.md) для быстрого начала работы
3. Попробуйте все режимы примерки (text, image, url, background)
4. Настройте дополнительные опции (themes, advanced settings)

## Получение помощи

Если возникли проблемы:

1. Проверьте [Troubleshooting](#устранение-проблем) выше
2. Посмотрите issues в репозитории (если проект на GitHub)
3. Проверьте логи в консоли браузера (F12)
4. Проверьте логи сервера в терминале

Удачи! 🚀

